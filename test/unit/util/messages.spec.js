import { assert } from 'chai';
import messages from '../../../util/messages';

describe('Messages utility test suite', () => {
  it('should exist', () => assert.isDefined(messages, 'Messages utility is defined'));
});
